import React from "react";
import StoreStyles from "./store.module.css";

const Store = () => {
  return <div className={`container-fluid ${StoreStyles.store}`}>
    
  </div>;
};

export default Store;
